{
  "id": "first-unique-character-in-a-string",
  "title": "First Unique Character In A String",
  "difficulty": "Easy",
  "tags": [
    "Queue",
    "Hash Table",
    "String",
    "Counting"
  ],
  "description": "Given a string s, find the first non-repeating character in it and return its index. If it does not exist, return -1.\n \nExample 1:\n\nInput: s = \"leetcode\"\nOutput: 0\nExplanation:\nThe character 'l' at index 0 is the first character that does not occur at any other index.\n\nExample 2:\n\nInput: s = \"loveleetcode\"\nOutput: 2\n\nExample 3:\n\nInput: s = \"aabb\"\nOutput: -1\n\n \nConstraints:\n\n1 <= s.length <= 105\ns consists of only lowercase English letters.\n\n",
  "entry_point": "Solution().firstUniqChar",
  "starter_code": "class Solution:\n    def firstUniqChar(self, s: str) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate('leetcode') == 0\n    assert candidate('loveleetcode') == 2\n    assert candidate('aabb') == -1\n    assert candidate('z') == 0\n    assert candidate('aaab') == 3\n    assert candidate('aabbccd') == 6\n    assert candidate('aabbccddeeffg') == 12\n    assert candidate('swiss') == 1",
  "test_cases": [
    "assert candidate('leetcode') == 0",
    "assert candidate('loveleetcode') == 2",
    "assert candidate('aabb') == -1",
    "assert candidate('z') == 0",
    "assert candidate('aaab') == 3",
    "assert candidate('aabbccd') == 6",
    "assert candidate('aabbccddeeffg') == 12",
    "assert candidate('swiss') == 1"
  ]
}
