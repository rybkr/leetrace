{
  "id": "largest-perimeter-triangle",
  "title": "Largest Perimeter Triangle",
  "difficulty": "Easy",
  "tags": [
    "Greedy",
    "Array",
    "Math",
    "Sorting"
  ],
  "description": "Given an integer array nums, return the largest perimeter of a triangle with a non-zero area, formed from three of these lengths. If it is impossible to form any triangle of a non-zero area, return 0.\n \nExample 1:\n\nInput: nums = [2,1,2]\nOutput: 5\nExplanation: You can form a triangle with three side lengths: 1, 2, and 2.\n\nExample 2:\n\nInput: nums = [1,2,1,10]\nOutput: 0\nExplanation: \nYou cannot use the side lengths 1, 1, and 2 to form a triangle.\nYou cannot use the side lengths 1, 1, and 10 to form a triangle.\nYou cannot use the side lengths 1, 2, and 10 to form a triangle.\nAs we cannot use any three side lengths to form a triangle of non-zero area, we return 0.\n\n \nConstraints:\n\n3 <= nums.length <= 104\n1 <= nums[i] <= 106\n\n",
  "entry_point": "Solution().largestPerimeter",
  "starter_code": "class Solution:\n    def largestPerimeter(self, nums: List[int]) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(nums=[2,1,2]) == 5\n    assert candidate(nums=[1,2,1,10]) == 0\n    assert candidate(nums=[3,2,3,4]) == 10\n    assert candidate(nums=[3,6,2,3]) == 8\n    assert candidate(nums=[10,15,20]) == 45\n    assert candidate(nums=[1,1,2,2,4,18,8]) == 5\n    assert candidate(nums=[5,5,5]) == 15\n    assert candidate(nums=[9,10,11,20]) == 41\n    assert candidate(nums=[1000000,1000000,1000000]) == 3000000\n    assert candidate(nums=[6,4,9,7,8]) == 24",
  "test_cases": [
    "assert candidate(nums=[2,1,2]) == 5",
    "assert candidate(nums=[1,2,1,10]) == 0",
    "assert candidate(nums=[3,2,3,4]) == 10",
    "assert candidate(nums=[3,6,2,3]) == 8",
    "assert candidate(nums=[10,15,20]) == 45",
    "assert candidate(nums=[1,1,2,2,4,18,8]) == 5",
    "assert candidate(nums=[5,5,5]) == 15",
    "assert candidate(nums=[9,10,11,20]) == 41",
    "assert candidate(nums=[1000000,1000000,1000000]) == 3000000",
    "assert candidate(nums=[6,4,9,7,8]) == 24"
  ]
}
