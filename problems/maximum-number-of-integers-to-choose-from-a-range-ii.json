{
  "id": "maximum-number-of-integers-to-choose-from-a-range-ii",
  "title": "Maximum Number Of Integers To Choose From A Range II",
  "difficulty": "Medium",
  "tags": [
    "Greedy",
    "Array",
    "Binary Search",
    "Sorting"
  ],
  "description": "You are given an integer array banned and two integers n and maxSum. You are choosing some number of integers following the below rules:\n\nThe chosen integers have to be in the range [1, n].\nEach integer can be chosen at most once.\nThe chosen integers should not be in the array banned.\nThe sum of the chosen integers should not exceed maxSum.\n\nReturn the maximum number of integers you can choose following the mentioned rules.\n \nExample 1:\n\nInput: banned = [1,4,6], n = 6, maxSum = 4\nOutput: 1\nExplanation: You can choose the integer 3.\n3 is in the range [1, 6], and do not appear in banned. The sum of the chosen integers is 3, which does not exceed maxSum.\n\nExample 2:\n\nInput: banned = [4,3,5,6], n = 7, maxSum = 18\nOutput: 3\nExplanation: You can choose the integers 1, 2, and 7.\nAll these integers are in the range [1, 7], all do not appear in banned, and their sum is 10, which does not exceed maxSum.\n\n \nConstraints:\n\n1 <= banned.length <= 105\n1 <= banned[i] <= n <= 109\n1 <= maxSum <= 1015\n\n",
  "entry_point": "Solution().maxCount",
  "starter_code": "class Solution:\n    def maxCount(self, banned: List[int], n: int, maxSum: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(banned = [1, 4, 6],n = 6,maxSum = 4) == 1\n    assert candidate(banned = [4, 3, 5, 6],n = 7,maxSum = 18) == 3\n    assert candidate(banned = [2, 3],n = 1000000000,maxSum = 10) == 3\n    assert candidate(banned = [1, 2, 3],n = 100,maxSum = 1000) == 41\n    assert candidate(banned = [100, 200, 300],n = 1000,maxSum = 6) == 3\n    assert candidate(banned = [1],n = 1000000000,maxSum = 1) == 0\n    assert candidate(banned = [5, 5, 5, 5],n = 10,maxSum = 15) == 4\n    assert candidate(banned = [999999999],n = 1000000000,maxSum = 3) == 2",
  "test_cases": [
    "assert candidate(banned = [1, 4, 6],n = 6,maxSum = 4) == 1",
    "assert candidate(banned = [4, 3, 5, 6],n = 7,maxSum = 18) == 3",
    "assert candidate(banned = [2, 3],n = 1000000000,maxSum = 10) == 3",
    "assert candidate(banned = [1, 2, 3],n = 100,maxSum = 1000) == 41",
    "assert candidate(banned = [100, 200, 300],n = 1000,maxSum = 6) == 3",
    "assert candidate(banned = [1],n = 1000000000,maxSum = 1) == 0",
    "assert candidate(banned = [5, 5, 5, 5],n = 10,maxSum = 15) == 4",
    "assert candidate(banned = [999999999],n = 1000000000,maxSum = 3) == 2"
  ]
}
