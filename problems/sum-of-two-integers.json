{
  "id": "sum-of-two-integers",
  "title": "Sum Of Two Integers",
  "difficulty": "Medium",
  "tags": [
    "Bit Manipulation",
    "Math"
  ],
  "description": "Given two integers a and b, return the sum of the two integers without using the operators + and -.\n \nExample 1:\nInput: a = 1, b = 2\nOutput: 3\nExample 2:\nInput: a = 2, b = 3\nOutput: 5\n\n \nConstraints:\n\n-1000 <= a, b <= 1000\n\n",
  "entry_point": "Solution().getSum",
  "starter_code": "class Solution:\n    def getSum(self, a: int, b: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(a=1, b=2) == 3\n    assert candidate(a=2, b=3) == 5\n    assert candidate(a=-2, b=3) == 1\n    assert candidate(a=-5, b=-7) == -12\n    assert candidate(a=0, b=0) == 0\n    assert candidate(a=-1000, b=1000) == 0\n    assert candidate(a=123, b=456) == 579\n    assert candidate(a=-15, b=7) == -8",
  "test_cases": [
    "assert candidate(a=1, b=2) == 3",
    "assert candidate(a=2, b=3) == 5",
    "assert candidate(a=-2, b=3) == 1",
    "assert candidate(a=-5, b=-7) == -12",
    "assert candidate(a=0, b=0) == 0",
    "assert candidate(a=-1000, b=1000) == 0",
    "assert candidate(a=123, b=456) == 579",
    "assert candidate(a=-15, b=7) == -8"
  ]
}
