{
  "id": "equal-tree-partition",
  "title": "Equal Tree Partition",
  "difficulty": "Medium",
  "tags": [
    "Tree",
    "Depth-First Search",
    "Binary Tree"
  ],
  "description": "Given the root of a binary tree, return true if you can partition the tree into two trees with equal sums of values after removing exactly one edge on the original tree.\n \nExample 1:\n\n\nInput: root = [5,10,10,null,null,2,3]\nOutput: true\n\nExample 2:\n\n\nInput: root = [1,2,10,null,null,2,20]\nOutput: false\nExplanation: You cannot split the tree into two trees with equal sums after removing exactly one edge on the tree.\n\n \nConstraints:\n\nThe number of nodes in the tree is in the range [1, 104].\n-105 <= Node.val <= 105\n\n",
  "entry_point": "Solution().checkEqualTree",
  "starter_code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def checkEqualTree(self, root: Optional[TreeNode]) -> bool:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(tree_node([5, 10, 10, None, None, 2, 3])) == True\n    assert candidate(tree_node([1, 2, 10, None, None, 2, 20])) == False\n    assert candidate(tree_node([0, 0])) == True\n    assert candidate(tree_node([1])) == False\n    assert candidate(tree_node([1, -1, 0])) == True\n    assert candidate(tree_node([2, 3, -5])) == False\n    assert candidate(tree_node([0, 1, -1])) == False\n    assert candidate(tree_node([10, 10])) == True",
  "test_cases": [
    "assert candidate(tree_node([5, 10, 10, None, None, 2, 3])) == True",
    "assert candidate(tree_node([1, 2, 10, None, None, 2, 20])) == False",
    "assert candidate(tree_node([0, 0])) == True",
    "assert candidate(tree_node([1])) == False",
    "assert candidate(tree_node([1, -1, 0])) == True",
    "assert candidate(tree_node([2, 3, -5])) == False",
    "assert candidate(tree_node([0, 1, -1])) == False",
    "assert candidate(tree_node([10, 10])) == True"
  ]
}
