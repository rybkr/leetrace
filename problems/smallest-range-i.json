{
  "id": "smallest-range-i",
  "title": "Smallest Range I",
  "difficulty": "Easy",
  "tags": [
    "Array",
    "Math"
  ],
  "description": "You are given an integer array nums and an integer k.\nIn one operation, you can choose any index i where 0 <= i < nums.length and change nums[i] to nums[i] + x where x is an integer from the range [-k, k]. You can apply this operation at most once for each index i.\nThe score of nums is the difference between the maximum and minimum elements in nums.\nReturn the minimum score of nums after applying the mentioned operation at most once for each index in it.\n \nExample 1:\n\nInput: nums = [1], k = 0\nOutput: 0\nExplanation: The score is max(nums) - min(nums) = 1 - 1 = 0.\n\nExample 2:\n\nInput: nums = [0,10], k = 2\nOutput: 6\nExplanation: Change nums to be [2, 8]. The score is max(nums) - min(nums) = 8 - 2 = 6.\n\nExample 3:\n\nInput: nums = [1,3,6], k = 3\nOutput: 0\nExplanation: Change nums to be [4, 4, 4]. The score is max(nums) - min(nums) = 4 - 4 = 0.\n\n \nConstraints:\n\n1 <= nums.length <= 104\n0 <= nums[i] <= 104\n0 <= k <= 104\n\n",
  "entry_point": "Solution().smallestRangeI",
  "starter_code": "class Solution:\n    def smallestRangeI(self, nums: List[int], k: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(nums=[1], k=0) == 0\n    assert candidate(nums=[0,10], k=2) == 6\n    assert candidate(nums=[1,3,6], k=3) == 0\n    assert candidate(nums=[1,2,3,4,5], k=1) == 2\n    assert candidate(nums=[5,5,5,5], k=10) == 0\n    assert candidate(nums=[4,8,15,16,23,42], k=10) == 18\n    assert candidate(nums=[0,100], k=60) == 0\n    assert candidate(nums=[2,6,8,10], k=1) == 6\n    assert candidate(nums=[1,10,14], k=3) == 7\n    assert candidate(nums=[100,200,300,400,500], k=150) == 100",
  "test_cases": [
    "assert candidate(nums=[1], k=0) == 0",
    "assert candidate(nums=[0,10], k=2) == 6",
    "assert candidate(nums=[1,3,6], k=3) == 0",
    "assert candidate(nums=[1,2,3,4,5], k=1) == 2",
    "assert candidate(nums=[5,5,5,5], k=10) == 0",
    "assert candidate(nums=[4,8,15,16,23,42], k=10) == 18",
    "assert candidate(nums=[0,100], k=60) == 0",
    "assert candidate(nums=[2,6,8,10], k=1) == 6",
    "assert candidate(nums=[1,10,14], k=3) == 7",
    "assert candidate(nums=[100,200,300,400,500], k=150) == 100"
  ]
}
