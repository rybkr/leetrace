{
  "id": "sort-array-by-parity-ii",
  "title": "Sort Array By Parity II",
  "difficulty": "Easy",
  "tags": [
    "Array",
    "Two Pointers",
    "Sorting"
  ],
  "description": "Given an array of integers nums, half of the integers in nums are odd, and the other half are even.\nSort the array so that whenever nums[i] is odd, i is odd, and whenever nums[i] is even, i is even.\nReturn any answer array that satisfies this condition.\n \nExample 1:\n\nInput: nums = [4,2,5,7]\nOutput: [4,5,2,7]\nExplanation: [4,7,2,5], [2,5,4,7], [2,7,4,5] would also have been accepted.\n\nExample 2:\n\nInput: nums = [2,3]\nOutput: [2,3]\n\n \nConstraints:\n\n2 <= nums.length <= 2 * 104\nnums.length is even.\nHalf of the integers in nums are even.\n0 <= nums[i] <= 1000\n\n \nFollow Up: Could you solve it in-place?\n",
  "entry_point": "Solution().sortArrayByParityII",
  "starter_code": "class Solution:\n    def sortArrayByParityII(self, nums: List[int]) -> List[int]:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert (res := candidate(nums=[4,2,5,7])) is not None and tuple(sorted(res)) in ((2,4,5,7),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[2,3])) is not None and tuple(sorted(res)) in ((2,3),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[3,4])) is not None and tuple(sorted(res)) in ((3,4),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[2,3,4,1])) is not None and tuple(sorted(res)) in ((1,2,3,4),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[1,2,3,4,5,6])) is not None and tuple(sorted(res)) in ((1,2,3,4,5,6),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[10,11,12,13,14,15])) is not None and tuple(sorted(res)) in ((10,11,12,13,14,15),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[2,2,1,1])) is not None and tuple(sorted(res)) in ((1,1,2,2),) and all(not ((res[i] - i) % 2) for i in range(len(res)))\n    assert (res := candidate(nums=[0,1,2,3,4,5,6,7])) is not None and tuple(sorted(res)) in ((0,1,2,3,4,5,6,7),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
  "test_cases": [
    "assert (res := candidate(nums=[4,2,5,7])) is not None and tuple(sorted(res)) in ((2,4,5,7),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[2,3])) is not None and tuple(sorted(res)) in ((2,3),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[3,4])) is not None and tuple(sorted(res)) in ((3,4),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[2,3,4,1])) is not None and tuple(sorted(res)) in ((1,2,3,4),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[1,2,3,4,5,6])) is not None and tuple(sorted(res)) in ((1,2,3,4,5,6),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[10,11,12,13,14,15])) is not None and tuple(sorted(res)) in ((10,11,12,13,14,15),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[2,2,1,1])) is not None and tuple(sorted(res)) in ((1,1,2,2),) and all(not ((res[i] - i) % 2) for i in range(len(res)))",
    "assert (res := candidate(nums=[0,1,2,3,4,5,6,7])) is not None and tuple(sorted(res)) in ((0,1,2,3,4,5,6,7),) and all(not ((res[i] - i) % 2) for i in range(len(res)))"
  ]
}
