{
  "id": "make-three-strings-equal",
  "title": "Make Three Strings Equal",
  "difficulty": "Easy",
  "tags": [
    "String"
  ],
  "description": "You are given three strings: s1, s2, and s3. In one operation you can choose one of these strings and delete its rightmost character. Note that you cannot completely empty a string.\nReturn the minimum number of operations required to make the strings equal. If it is impossible to make them equal, return -1.\n \nExample 1:\n\nInput: s1 = \"abc\", s2 = \"abb\", s3 = \"ab\"\nOutput: 2\nExplanation: Deleting the rightmost character from both s1 and s2 will result in three equal strings.\n\nExample 2:\n\nInput: s1 = \"dac\", s2 = \"bac\", s3 = \"cac\"\nOutput: -1\nExplanation: Since the first letters of s1 and s2 differ, they cannot be made equal.\n\n \nConstraints:\n\n1 <= s1.length, s2.length, s3.length <= 100\ns1, s2 and s3 consist only of lowercase English letters.\n\n",
  "entry_point": "Solution().findMinimumOperations",
  "starter_code": "class Solution:\n    def findMinimumOperations(self, s1: str, s2: str, s3: str) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate('abc', 'abb', 'ab') == 2\n    assert candidate('dac', 'bac', 'cac') == -1\n    assert candidate('aaa', 'aaa', 'aaa') == 0\n    assert candidate('a', 'a', 'a') == 0\n    assert candidate('abcd', 'ab', 'abc') == 3\n    assert candidate('x', 'xy', 'xyz') == 3\n    assert candidate('leetcode', 'leet', 'lee') == 6\n    assert candidate('abc', 'abc', 'abx') == 3",
  "test_cases": [
    "assert candidate('abc', 'abb', 'ab') == 2",
    "assert candidate('dac', 'bac', 'cac') == -1",
    "assert candidate('aaa', 'aaa', 'aaa') == 0",
    "assert candidate('a', 'a', 'a') == 0",
    "assert candidate('abcd', 'ab', 'abc') == 3",
    "assert candidate('x', 'xy', 'xyz') == 3",
    "assert candidate('leetcode', 'leet', 'lee') == 6",
    "assert candidate('abc', 'abc', 'abx') == 3"
  ]
}
