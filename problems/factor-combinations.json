{
  "id": "factor-combinations",
  "title": "Factor Combinations",
  "difficulty": "Medium",
  "tags": [
    "Backtracking"
  ],
  "description": "Numbers can be regarded as the product of their factors.\n\nFor example, 8 = 2 x 2 x 2 = 2 x 4.\n\nGiven an integer n, return all possible combinations of its factors. You may return the answer in any order.\nNote that the factors should be in the range [2, n - 1].\n \nExample 1:\n\nInput: n = 1\nOutput: []\n\nExample 2:\n\nInput: n = 12\nOutput: [[2,6],[3,4],[2,2,3]]\n\nExample 3:\n\nInput: n = 37\nOutput: []\n\n \nConstraints:\n\n1 <= n <= 107\n\n",
  "entry_point": "Solution().getFactors",
  "starter_code": "class Solution:\n    def getFactors(self, n: int) -> List[List[int]]:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert sorted(sorted(group) for group in candidate(1)) == []\n    assert sorted(sorted(group) for group in candidate(12)) == [[2,2,3],[2,6],[3,4]]\n    assert sorted(sorted(group) for group in candidate(37)) == []\n    assert sorted(sorted(group) for group in candidate(32)) == [[2,2,2,2,2],[2,2,2,4],[2,2,8],[2,4,4],[2,16],[4,8]]\n    assert sorted(sorted(group) for group in candidate(16)) == [[2,2,2,2],[2,2,4],[2,8],[4,4]]\n    assert sorted(sorted(group) for group in candidate(24)) == [[2,2,2,3],[2,2,6],[2,3,4],[2,12],[3,8],[4,6]]\n    assert sorted(sorted(group) for group in candidate(17)) == []\n    assert sorted(sorted(group) for group in candidate(81)) == [[3,3,3,3],[3,3,9],[3,27],[9,9]]",
  "test_cases": [
    "assert sorted(sorted(group) for group in candidate(1)) == []",
    "assert sorted(sorted(group) for group in candidate(12)) == [[2,2,3],[2,6],[3,4]]",
    "assert sorted(sorted(group) for group in candidate(37)) == []",
    "assert sorted(sorted(group) for group in candidate(32)) == [[2,2,2,2,2],[2,2,2,4],[2,2,8],[2,4,4],[2,16],[4,8]]",
    "assert sorted(sorted(group) for group in candidate(16)) == [[2,2,2,2],[2,2,4],[2,8],[4,4]]",
    "assert sorted(sorted(group) for group in candidate(24)) == [[2,2,2,3],[2,2,6],[2,3,4],[2,12],[3,8],[4,6]]",
    "assert sorted(sorted(group) for group in candidate(17)) == []",
    "assert sorted(sorted(group) for group in candidate(81)) == [[3,3,3,3],[3,3,9],[3,27],[9,9]]"
  ]
}
