{
  "id": "minimum-consecutive-cards-to-pick-up",
  "title": "Minimum Consecutive Cards To Pick Up",
  "difficulty": "Medium",
  "tags": [
    "Array",
    "Hash Table",
    "Sliding Window"
  ],
  "description": "You are given an integer array cards where cards[i] represents the value of the ith card. A pair of cards are matching if the cards have the same value.\nReturn the minimum number of consecutive cards you have to pick up to have a pair of matching cards among the picked cards. If it is impossible to have matching cards, return -1.\n \nExample 1:\n\nInput: cards = [3,4,2,3,4,7]\nOutput: 4\nExplanation: We can pick up the cards [3,4,2,3] which contain a matching pair of cards with value 3. Note that picking up the cards [4,2,3,4] is also optimal.\n\nExample 2:\n\nInput: cards = [1,0,5,3]\nOutput: -1\nExplanation: There is no way to pick up a set of consecutive cards that contain a pair of matching cards.\n\n \nConstraints:\n\n1 <= cards.length <= 105\n0 <= cards[i] <= 106\n\n",
  "entry_point": "Solution().minimumCardPickup",
  "starter_code": "class Solution:\n    def minimumCardPickup(self, cards: List[int]) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate([3, 4, 2, 3, 4, 7]) == 4\n    assert candidate([1, 0, 5, 3]) == -1\n    assert candidate([1, 1]) == 2\n    assert candidate([1, 2, 3, 1, 2, 3]) == 4\n    assert candidate([1, 2, 3, 4]) == -1\n    assert candidate([5, 6, 5, 6, 5]) == 3\n    assert candidate([1, 2, 1, 1]) == 2\n    assert candidate([0, 1, 2, 3, 0]) == 5",
  "test_cases": [
    "assert candidate([3, 4, 2, 3, 4, 7]) == 4",
    "assert candidate([1, 0, 5, 3]) == -1",
    "assert candidate([1, 1]) == 2",
    "assert candidate([1, 2, 3, 1, 2, 3]) == 4",
    "assert candidate([1, 2, 3, 4]) == -1",
    "assert candidate([5, 6, 5, 6, 5]) == 3",
    "assert candidate([1, 2, 1, 1]) == 2",
    "assert candidate([0, 1, 2, 3, 0]) == 5"
  ]
}
