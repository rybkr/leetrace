{
  "id": "largest-triangle-area",
  "title": "Largest Triangle Area",
  "difficulty": "Easy",
  "tags": [
    "Geometry",
    "Array",
    "Math"
  ],
  "description": "Given an array of points on the X-Y plane points where points[i] = [xi, yi], return the area of the largest triangle that can be formed by any three different points. Answers within 10-5 of the actual answer will be accepted.\n \nExample 1:\n\n\nInput: points = [[0,0],[0,1],[1,0],[0,2],[2,0]]\nOutput: 2.00000\nExplanation: The five points are shown in the above figure. The red triangle is the largest.\n\nExample 2:\n\nInput: points = [[1,0],[0,0],[0,1]]\nOutput: 0.50000\n\n \nConstraints:\n\n3 <= points.length <= 50\n-50 <= xi, yi <= 50\nAll the given points are unique.\n\n",
  "entry_point": "Solution().largestTriangleArea",
  "starter_code": "class Solution:\n    def largestTriangleArea(self, points: List[List[int]]) -> float:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert abs(candidate(points = [[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]]) - 2.0) < 1e-5\n    assert abs(candidate(points = [[1, 0], [0, 0], [0, 1]]) - 0.5) < 1e-5\n    assert abs(candidate(points = [[0, 0], [1, 1], [2, 2]]) - 0.0) < 1e-5\n    assert abs(candidate(points = [[-50, -50], [50, 50], [-50, 50], [50, -50], [0, 0]]) - 5000.0) < 1e-5\n    assert abs(candidate(points = [[0, 0], [10, 0], [0, 10], [5, 5], [3, 7]]) - 50.0) < 1e-5\n    assert abs(candidate(points = [[-3, -3], [-2, -2], [-1, -1], [0, 0], [1, 1], [2, 2], [3, 3], [-3, 3]]) - 18.0) < 1e-5\n    assert abs(candidate(points = [[-25, 25], [-50, 0], [0, 0], [25, 0], [50, 0], [0, -25], [0, 25], [25, 25], [-25, 0]]) - 1562.5) < 1e-5\n    assert abs(candidate(points = [[0, 0], [2, 0], [0, 3], [1, 1], [2, 2]]) - 3.0) < 1e-5",
  "test_cases": [
    "assert abs(candidate(points = [[0, 0], [0, 1], [1, 0], [0, 2], [2, 0]]) - 2.0) < 1e-5",
    "assert abs(candidate(points = [[1, 0], [0, 0], [0, 1]]) - 0.5) < 1e-5",
    "assert abs(candidate(points = [[0, 0], [1, 1], [2, 2]]) - 0.0) < 1e-5",
    "assert abs(candidate(points = [[-50, -50], [50, 50], [-50, 50], [50, -50], [0, 0]]) - 5000.0) < 1e-5",
    "assert abs(candidate(points = [[0, 0], [10, 0], [0, 10], [5, 5], [3, 7]]) - 50.0) < 1e-5",
    "assert abs(candidate(points = [[-3, -3], [-2, -2], [-1, -1], [0, 0], [1, 1], [2, 2], [3, 3], [-3, 3]]) - 18.0) < 1e-5",
    "assert abs(candidate(points = [[-25, 25], [-50, 0], [0, 0], [25, 0], [50, 0], [0, -25], [0, 25], [25, 25], [-25, 0]]) - 1562.5) < 1e-5",
    "assert abs(candidate(points = [[0, 0], [2, 0], [0, 3], [1, 1], [2, 2]]) - 3.0) < 1e-5"
  ]
}
