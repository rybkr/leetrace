{
  "id": "shortest-subarray-with-or-at-least-k-ii",
  "title": "Shortest Subarray With Or At Least K II",
  "difficulty": "Medium",
  "tags": [
    "Bit Manipulation",
    "Array",
    "Sliding Window"
  ],
  "description": "You are given an array nums of non-negative integers and an integer k.\nAn array is called special if the bitwise OR of all of its elements is at least k.\nReturn the length of the shortest special non-empty subarray of nums, or return -1 if no special subarray exists.\n \nExample 1:\n\nInput: nums = [1,2,3], k = 2\nOutput: 1\nExplanation:\nThe subarray [3] has OR value of 3. Hence, we return 1.\n\nExample 2:\n\nInput: nums = [2,1,8], k = 10\nOutput: 3\nExplanation:\nThe subarray [2,1,8] has OR value of 11. Hence, we return 3.\n\nExample 3:\n\nInput: nums = [1,2], k = 0\nOutput: 1\nExplanation:\nThe subarray [1] has OR value of 1. Hence, we return 1.\n\n \nConstraints:\n\n1 <= nums.length <= 2 * 105\n0 <= nums[i] <= 109\n0 <= k <= 109\n\n",
  "entry_point": "Solution().minimumSubarrayLength",
  "starter_code": "class Solution:\n    def minimumSubarrayLength(self, nums: List[int], k: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(nums=[1, 2, 3], k=2) == 1\n    assert candidate(nums=[2, 1, 8], k=10) == 3\n    assert candidate(nums=[1, 2], k=0) == 1\n    assert candidate(nums=[0, 0, 0], k=1) == -1\n    assert candidate(nums=[1, 2, 4, 8], k=7) == 1\n    assert candidate(nums=[1, 2, 4], k=7) == 3\n    assert candidate(nums=[5, 1, 1], k=7) == -1\n    assert candidate(nums=[3, 5, 1, 2], k=7) == 2",
  "test_cases": [
    "assert candidate(nums=[1, 2, 3], k=2) == 1",
    "assert candidate(nums=[2, 1, 8], k=10) == 3",
    "assert candidate(nums=[1, 2], k=0) == 1",
    "assert candidate(nums=[0, 0, 0], k=1) == -1",
    "assert candidate(nums=[1, 2, 4, 8], k=7) == 1",
    "assert candidate(nums=[1, 2, 4], k=7) == 3",
    "assert candidate(nums=[5, 1, 1], k=7) == -1",
    "assert candidate(nums=[3, 5, 1, 2], k=7) == 2"
  ]
}
