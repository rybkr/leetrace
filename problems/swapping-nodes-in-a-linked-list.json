{
  "id": "swapping-nodes-in-a-linked-list",
  "title": "Swapping Nodes In A Linked List",
  "difficulty": "Medium",
  "tags": [
    "Linked List",
    "Two Pointers"
  ],
  "description": "You are given the head of a linked list, and an integer k.\nReturn the head of the linked list after swapping the values of the kth node from the beginning and the kth node from the end (the list is 1-indexed).\n \nExample 1:\n\n\nInput: head = [1,2,3,4,5], k = 2\nOutput: [1,4,3,2,5]\n\nExample 2:\n\nInput: head = [7,9,6,6,7,8,3,0,9,5], k = 5\nOutput: [7,9,6,6,8,7,3,0,9,5]\n\n \nConstraints:\n\nThe number of nodes in the list is n.\n1 <= k <= n <= 105\n0 <= Node.val <= 100\n\n",
  "entry_point": "Solution().swapNodes",
  "starter_code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    def swapNodes(self, head: Optional[ListNode], k: int) -> Optional[ListNode]:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert is_same_list(candidate(list_node([1, 2, 3, 4, 5]), 2), list_node([1, 4, 3, 2, 5]))\n    assert is_same_list(candidate(list_node([7, 9, 6, 6, 7, 8, 3, 0, 9, 5]), 5), list_node([7, 9, 6, 6, 8, 7, 3, 0, 9, 5]))\n    assert is_same_list(candidate(list_node([1]), 1), list_node([1]))\n    assert is_same_list(candidate(list_node([1, 2]), 1), list_node([2, 1]))\n    assert is_same_list(candidate(list_node([1, 2]), 2), list_node([2, 1]))\n    assert is_same_list(candidate(list_node([1, 2, 3]), 2), list_node([1, 2, 3]))\n    assert is_same_list(candidate(list_node([1, 2, 3, 4]), 1), list_node([4, 2, 3, 1]))\n    assert is_same_list(candidate(list_node([1, 2, 3, 4]), 4), list_node([4, 2, 3, 1]))",
  "test_cases": [
    "assert is_same_list(candidate(list_node([1, 2, 3, 4, 5]), 2), list_node([1, 4, 3, 2, 5]))",
    "assert is_same_list(candidate(list_node([7, 9, 6, 6, 7, 8, 3, 0, 9, 5]), 5), list_node([7, 9, 6, 6, 8, 7, 3, 0, 9, 5]))",
    "assert is_same_list(candidate(list_node([1]), 1), list_node([1]))",
    "assert is_same_list(candidate(list_node([1, 2]), 1), list_node([2, 1]))",
    "assert is_same_list(candidate(list_node([1, 2]), 2), list_node([2, 1]))",
    "assert is_same_list(candidate(list_node([1, 2, 3]), 2), list_node([1, 2, 3]))",
    "assert is_same_list(candidate(list_node([1, 2, 3, 4]), 1), list_node([4, 2, 3, 1]))",
    "assert is_same_list(candidate(list_node([1, 2, 3, 4]), 4), list_node([4, 2, 3, 1]))"
  ]
}
