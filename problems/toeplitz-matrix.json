{
  "id": "toeplitz-matrix",
  "title": "Toeplitz Matrix",
  "difficulty": "Easy",
  "tags": [
    "Array",
    "Matrix"
  ],
  "description": "Given an m x n matrix, return true if the matrix is Toeplitz. Otherwise, return false.\nA matrix is Toeplitz if every diagonal from top-left to bottom-right has the same elements.\n \nExample 1:\n\n\nInput: matrix = [[1,2,3,4],[5,1,2,3],[9,5,1,2]]\nOutput: true\nExplanation:\nIn the above grid, the diagonals are:\n\"[9]\", \"[5, 5]\", \"[1, 1, 1]\", \"[2, 2, 2]\", \"[3, 3]\", \"[4]\".\nIn each diagonal all elements are the same, so the answer is True.\n\nExample 2:\n\n\nInput: matrix = [[1,2],[2,2]]\nOutput: false\nExplanation:\nThe diagonal \"[1, 2]\" has different elements.\n\n \nConstraints:\n\nm == matrix.length\nn == matrix[i].length\n1 <= m, n <= 20\n0 <= matrix[i][j] <= 99\n\n \nFollow up:\n\nWhat if the matrix is stored on disk, and the memory is limited such that you can only load at most one row of the matrix into the memory at once?\nWhat if the matrix is so large that you can only load up a partial row into the memory at once?\n\n",
  "entry_point": "Solution().isToeplitzMatrix",
  "starter_code": "class Solution:\n    def isToeplitzMatrix(self, matrix: List[List[int]]) -> bool:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(matrix = [[1, 2, 3, 4], [5, 1, 2, 3], [9, 5, 1, 2]]) == True\n    assert candidate(matrix = [[1, 2], [2, 2]]) == False\n    assert candidate(matrix = [[7]]) == True\n    assert candidate(matrix = [[3, 4, 5], [6, 3, 4], [7, 6, 3], [8, 7, 6]]) == True\n    assert candidate(matrix = [[1, 1, 1], [1, 1, 2], [1, 1, 1]]) == False\n    assert candidate(matrix = [[5, 6, 7, 8], [4, 5, 6, 7], [3, 4, 5, 6], [2, 3, 4, 5], [1, 2, 3, 4]]) == True\n    assert candidate(matrix = [[0, 1, 2, 3], [4, 0, 1, 9], [5, 4, 0, 1]]) == False\n    assert candidate(matrix = [[9], [8], [7], [6]]) == True",
  "test_cases": [
    "assert candidate(matrix = [[1, 2, 3, 4], [5, 1, 2, 3], [9, 5, 1, 2]]) == True",
    "assert candidate(matrix = [[1, 2], [2, 2]]) == False",
    "assert candidate(matrix = [[7]]) == True",
    "assert candidate(matrix = [[3, 4, 5], [6, 3, 4], [7, 6, 3], [8, 7, 6]]) == True",
    "assert candidate(matrix = [[1, 1, 1], [1, 1, 2], [1, 1, 1]]) == False",
    "assert candidate(matrix = [[5, 6, 7, 8], [4, 5, 6, 7], [3, 4, 5, 6], [2, 3, 4, 5], [1, 2, 3, 4]]) == True",
    "assert candidate(matrix = [[0, 1, 2, 3], [4, 0, 1, 9], [5, 4, 0, 1]]) == False",
    "assert candidate(matrix = [[9], [8], [7], [6]]) == True"
  ]
}
