{
  "id": "find-the-smallest-divisor-given-a-threshold",
  "title": "Find The Smallest Divisor Given A Threshold",
  "difficulty": "Medium",
  "tags": [
    "Array",
    "Binary Search"
  ],
  "description": "Given an array of integers nums and an integer threshold, we will choose a positive integer divisor, divide all the array by it, and sum the division's result. Find the smallest divisor such that the result mentioned above is less than or equal to threshold.\nEach result of the division is rounded to the nearest integer greater than or equal to that element. (For example: 7/3 = 3 and 10/2 = 5).\nThe test cases are generated so that there will be an answer.\n \nExample 1:\n\nInput: nums = [1,2,5,9], threshold = 6\nOutput: 5\nExplanation: We can get a sum to 17 (1+2+5+9) if the divisor is 1. \nIf the divisor is 4 we can get a sum of 7 (1+1+2+3) and if the divisor is 5 the sum will be 5 (1+1+1+2). \n\nExample 2:\n\nInput: nums = [44,22,33,11,1], threshold = 5\nOutput: 44\n\n \nConstraints:\n\n1 <= nums.length <= 5 * 104\n1 <= nums[i] <= 106\nnums.length <= threshold <= 106\n\n",
  "entry_point": "Solution().smallestDivisor",
  "starter_code": "class Solution:\n    def smallestDivisor(self, nums: List[int], threshold: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(nums = [1, 2, 5, 9], threshold = 6) == 5\n    assert candidate(nums = [44, 22, 33, 11, 1], threshold = 5) == 44\n    assert candidate(nums = [2, 3, 5, 7, 11], threshold = 11) == 3\n    assert candidate(nums = [19], threshold = 5) == 4\n    assert candidate(nums = [8, 4, 2], threshold = 8) == 2\n    assert candidate(nums = [7, 17, 29], threshold = 7) == 9\n    assert candidate(nums = [21212, 10101, 12121], threshold = 1000000) == 1\n    assert candidate(nums = [1000000, 1000000], threshold = 3) == 1000000",
  "test_cases": [
    "assert candidate(nums = [1, 2, 5, 9], threshold = 6) == 5",
    "assert candidate(nums = [44, 22, 33, 11, 1], threshold = 5) == 44",
    "assert candidate(nums = [2, 3, 5, 7, 11], threshold = 11) == 3",
    "assert candidate(nums = [19], threshold = 5) == 4",
    "assert candidate(nums = [8, 4, 2], threshold = 8) == 2",
    "assert candidate(nums = [7, 17, 29], threshold = 7) == 9",
    "assert candidate(nums = [21212, 10101, 12121], threshold = 1000000) == 1",
    "assert candidate(nums = [1000000, 1000000], threshold = 3) == 1000000"
  ]
}
