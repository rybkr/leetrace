{
  "id": "missing-element-in-sorted-array",
  "title": "Missing Element In Sorted Array",
  "difficulty": "Medium",
  "tags": [
    "Array",
    "Binary Search"
  ],
  "description": "Given an integer array nums which is sorted in ascending order and all of its elements are unique and given also an integer k, return the kth missing number starting from the leftmost number of the array.\n \nExample 1:\n\nInput: nums = [4,7,9,10], k = 1\nOutput: 5\nExplanation: The first missing number is 5.\n\nExample 2:\n\nInput: nums = [4,7,9,10], k = 3\nOutput: 8\nExplanation: The missing numbers are [5,6,8,...], hence the third missing number is 8.\n\nExample 3:\n\nInput: nums = [1,2,4], k = 3\nOutput: 6\nExplanation: The missing numbers are [3,5,6,7,...], hence the third missing number is 6.\n\n \nConstraints:\n\n1 <= nums.length <= 5 * 104\n1 <= nums[i] <= 107\nnums is sorted in ascending order, and all the elements are unique.\n1 <= k <= 108\n\n \nFollow up: Can you find a logarithmic time complexity (i.e., O(log(n))) solution?",
  "entry_point": "Solution().missingElement",
  "starter_code": "class Solution:\n    def missingElement(self, nums: List[int], k: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(nums = [4, 7, 9, 10], k = 1) == 5\n    assert candidate(nums = [4, 7, 9, 10], k = 3) == 8\n    assert candidate(nums = [1, 2, 4], k = 3) == 6\n    assert candidate(nums = [2, 3, 5, 9], k = 1) == 4\n    assert candidate(nums = [2, 3, 5, 9], k = 4) == 8\n    assert candidate(nums = [1, 5], k = 1) == 2\n    assert candidate(nums = [1, 5], k = 6) == 8\n    assert candidate(nums = [10, 11, 12], k = 1) == 13\n    assert candidate(nums = [100, 101, 104, 107], k = 5) == 108",
  "test_cases": [
    "assert candidate(nums = [4, 7, 9, 10], k = 1) == 5",
    "assert candidate(nums = [4, 7, 9, 10], k = 3) == 8",
    "assert candidate(nums = [1, 2, 4], k = 3) == 6",
    "assert candidate(nums = [2, 3, 5, 9], k = 1) == 4",
    "assert candidate(nums = [2, 3, 5, 9], k = 4) == 8",
    "assert candidate(nums = [1, 5], k = 1) == 2",
    "assert candidate(nums = [1, 5], k = 6) == 8",
    "assert candidate(nums = [10, 11, 12], k = 1) == 13",
    "assert candidate(nums = [100, 101, 104, 107], k = 5) == 108"
  ]
}
