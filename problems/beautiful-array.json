{
  "id": "beautiful-array",
  "title": "Beautiful Array",
  "difficulty": "Medium",
  "tags": [
    "Array",
    "Math",
    "Divide and Conquer"
  ],
  "description": "An array nums of length n is beautiful if:\n\nnums is a permutation of the integers in the range [1, n].\nFor every 0 <= i < j < n, there is no index k with i < k < j where 2 * nums[k] == nums[i] + nums[j].\n\nGiven the integer n, return any beautiful array nums of length n. There will be at least one valid answer for the given n.\n \nExample 1:\nInput: n = 4\nOutput: [2,1,4,3]\nExample 2:\nInput: n = 5\nOutput: [3,1,2,5,4]\n\n \nConstraints:\n\n1 <= n <= 1000\n\n",
  "entry_point": "Solution().beautifulArray",
  "starter_code": "class Solution:\n    def beautifulArray(self, n: int) -> List[int]:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert (arr := candidate(1)) and len(arr) in [1] and set(arr) >= {1} and set(arr) <= {1} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(2)) and len(arr) in [2] and set(arr) >= {1,2} and set(arr) <= {1,2} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(4)) and len(arr) in [4] and set(arr) >= {1,2,3,4} and set(arr) <= {1,2,3,4} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(5)) and len(arr) in [5] and set(arr) >= {1,2,3,4,5} and set(arr) <= {1,2,3,4,5} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(8)) and len(arr) in [8] and set(arr) >= set(range(1, 9)) and set(arr) <= set(range(1, 9)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(10)) and len(arr) in [10] and set(arr) >= set(range(1, 11)) and set(arr) <= set(range(1, 11)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(25)) and len(arr) in [25] and set(arr) >= set(range(1, 26)) and set(arr) <= set(range(1, 26)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))\n    assert (arr := candidate(50)) and len(arr) in [50] and set(arr) >= set(range(1, 51)) and set(arr) <= set(range(1, 51)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
  "test_cases": [
    "assert (arr := candidate(1)) and len(arr) in [1] and set(arr) >= {1} and set(arr) <= {1} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(2)) and len(arr) in [2] and set(arr) >= {1,2} and set(arr) <= {1,2} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(4)) and len(arr) in [4] and set(arr) >= {1,2,3,4} and set(arr) <= {1,2,3,4} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(5)) and len(arr) in [5] and set(arr) >= {1,2,3,4,5} and set(arr) <= {1,2,3,4,5} and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(8)) and len(arr) in [8] and set(arr) >= set(range(1, 9)) and set(arr) <= set(range(1, 9)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(10)) and len(arr) in [10] and set(arr) >= set(range(1, 11)) and set(arr) <= set(range(1, 11)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(25)) and len(arr) in [25] and set(arr) >= set(range(1, 26)) and set(arr) <= set(range(1, 26)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))",
    "assert (arr := candidate(50)) and len(arr) in [50] and set(arr) >= set(range(1, 51)) and set(arr) <= set(range(1, 51)) and all((2 * arr[k]) not in [arr[i] + arr[j]] for i in range(len(arr)) for j in range(i + 1, len(arr)) for k in range(i + 1, j))"
  ]
}
