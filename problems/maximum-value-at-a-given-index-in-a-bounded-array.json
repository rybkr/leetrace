{
  "id": "maximum-value-at-a-given-index-in-a-bounded-array",
  "title": "Maximum Value At A Given Index In A Bounded Array",
  "difficulty": "Medium",
  "tags": [
    "Greedy",
    "Binary Search"
  ],
  "description": "You are given three positive integers: n, index, and maxSum. You want to construct an array nums (0-indexed) that satisfies the following conditions:\n\nnums.length == n\nnums[i] is a positive integer where 0 <= i < n.\nabs(nums[i] - nums[i+1]) <= 1 where 0 <= i < n-1.\nThe sum of all the elements of nums does not exceed maxSum.\nnums[index] is maximized.\n\nReturn nums[index] of the constructed array.\nNote that abs(x) equals x if x >= 0, and -x otherwise.\n \nExample 1:\n\nInput: n = 4, index = 2,  maxSum = 6\nOutput: 2\nExplanation: nums = [1,2,2,1] is one array that satisfies all the conditions.\nThere are no arrays that satisfy all the conditions and have nums[2] == 3, so 2 is the maximum nums[2].\n\nExample 2:\n\nInput: n = 6, index = 1,  maxSum = 10\nOutput: 3\n\n \nConstraints:\n\n1 <= n <= maxSum <= 109\n0 <= index < n\n\n",
  "entry_point": "Solution().maxValue",
  "starter_code": "class Solution:\n    def maxValue(self, n: int, index: int, maxSum: int) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(n=4, index=2, maxSum=6) == 2\n    assert candidate(n=6, index=1, maxSum=10) == 3\n    assert candidate(n=1, index=0, maxSum=1) == 1\n    assert candidate(n=1, index=0, maxSum=1000000000) == 1000000000\n    assert candidate(n=4, index=0, maxSum=4) == 1\n    assert candidate(n=4, index=3, maxSum=10) == 4\n    assert candidate(n=3, index=1, maxSum=18) == 6\n    assert candidate(n=8, index=3, maxSum=30) == 5\n    assert candidate(n=10, index=5, maxSum=100) == 12\n    assert candidate(n=1000000000, index=500000000, maxSum=1000000000) == 1\n    assert candidate(n=1000000000, index=0, maxSum=2000000000) == 44721",
  "test_cases": [
    "assert candidate(n=4, index=2, maxSum=6) == 2",
    "assert candidate(n=6, index=1, maxSum=10) == 3",
    "assert candidate(n=1, index=0, maxSum=1) == 1",
    "assert candidate(n=1, index=0, maxSum=1000000000) == 1000000000",
    "assert candidate(n=4, index=0, maxSum=4) == 1",
    "assert candidate(n=4, index=3, maxSum=10) == 4",
    "assert candidate(n=3, index=1, maxSum=18) == 6",
    "assert candidate(n=8, index=3, maxSum=30) == 5",
    "assert candidate(n=10, index=5, maxSum=100) == 12",
    "assert candidate(n=1000000000, index=500000000, maxSum=1000000000) == 1",
    "assert candidate(n=1000000000, index=0, maxSum=2000000000) == 44721"
  ]
}
