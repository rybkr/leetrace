{
  "id": "maximum-distance-in-arrays",
  "title": "Maximum Distance In Arrays",
  "difficulty": "Medium",
  "tags": [
    "Greedy",
    "Array"
  ],
  "description": "You are given m arrays, where each array is sorted in ascending order.\nYou can pick up two integers from two different arrays (each array picks one) and calculate the distance. We define the distance between two integers a and b to be their absolute difference |a - b|.\nReturn the maximum distance.\n \nExample 1:\n\nInput: arrays = [[1,2,3],[4,5],[1,2,3]]\nOutput: 4\nExplanation: One way to reach the maximum distance 4 is to pick 1 in the first or third array and pick 5 in the second array.\n\nExample 2:\n\nInput: arrays = [[1],[1]]\nOutput: 0\n\n \nConstraints:\n\nm == arrays.length\n2 <= m <= 105\n1 <= arrays[i].length <= 500\n-104 <= arrays[i][j] <= 104\narrays[i] is sorted in ascending order.\nThere will be at most 105 integers in all the arrays.\n\n",
  "entry_point": "Solution().maxDistance",
  "starter_code": "class Solution:\n    def maxDistance(self, arrays: List[List[int]]) -> int:\n        ",
  "preamble": "from typing import *\nfrom collections import *\nfrom functools import *\nfrom itertools import *\nfrom heapq import *\nfrom bisect import *\nfrom math import *\nimport math, collections, functools, itertools, bisect, heapq, string, re\n",
  "check_function": "def check(candidate):\n    assert candidate(arrays=[[1, 2, 3], [4, 5], [1, 2, 3]]) == 4\n    assert candidate(arrays=[[1], [1]]) == 0\n    assert candidate(arrays=[[-10, -5, 0], [3, 4], [5, 6, 7]]) == 17\n    assert candidate(arrays=[[1, 4], [0, 5]]) == 4\n    assert candidate(arrays=[[-1, 1], [-3, 1, 4], [-2, -1, 0, 2]]) == 6\n    assert candidate(arrays=[[1, 5], [3, 4]]) == 3\n    assert candidate(arrays=[[1, 2], [1, 2, 3]]) == 2\n    assert candidate(arrays=[[1], [2], [3]]) == 2",
  "test_cases": [
    "assert candidate(arrays=[[1, 2, 3], [4, 5], [1, 2, 3]]) == 4",
    "assert candidate(arrays=[[1], [1]]) == 0",
    "assert candidate(arrays=[[-10, -5, 0], [3, 4], [5, 6, 7]]) == 17",
    "assert candidate(arrays=[[1, 4], [0, 5]]) == 4",
    "assert candidate(arrays=[[-1, 1], [-3, 1, 4], [-2, -1, 0, 2]]) == 6",
    "assert candidate(arrays=[[1, 5], [3, 4]]) == 3",
    "assert candidate(arrays=[[1, 2], [1, 2, 3]]) == 2",
    "assert candidate(arrays=[[1], [2], [3]]) == 2"
  ]
}
